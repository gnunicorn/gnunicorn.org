image: ruby:2.3

pages:
  script:
    # we need node to execute this
  - apt-get update -qq && apt-get install -qq nodejs
  - bundle install --path vendor
  - LANG=en_US.UTF-8 bundle exec jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - pages